{{#tag}}
<div class="row">
  <header class="post-full-header" style="{{#if image}}background:url({{image}}) no-repeat center center;background-size:cover;{{/if}}">
    <div class="container">
      <h1 class="post-title">Posts tagged with {{name}}</h1>
    <div>
  </header>
</div>
{{/tag}}
