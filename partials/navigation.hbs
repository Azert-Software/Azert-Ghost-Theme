<div class="navbar-fixed">
  <nav class="black-text z-depth-0">
    <div class="container">
     <div class="nav-wrapper">
       <a class="brand-logo white-text" href="{{@blog.url}}"><img src="{{@blog.logo}}"/></a>
       <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
       <ul class="right hide-on-med-and-down">
         {{#foreach navigation}}
             <li class="nav-{{slug}}{{#if current}} active{{/if}}" role="presentation"><a class="white-text" href="{{url absolute="true"}}">{{label}}</a></li>
         {{/foreach}}
       </ul>
      </div>
    </div>
   </nav>
 </div>
